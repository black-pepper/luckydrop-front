// 더미 데이터 파일 — API 연동 완료 후 더 이상 사용되지 않음.
// 모든 mock 데이터(dummyUsers, prizes, rewards, getRandomPrize, findUser)는
// src/api/client.ts의 실제 API 호출로 대체되었습니다.
//
// 제거된 mock:
// - dummyUsers / findUser → verifyCode API
// - prizes / getRandomPrize → executeDraw API
// - rewards → getRewards API
// - DrawResult / formatTime → getResults API

export {};
