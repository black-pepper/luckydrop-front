파일명:
- 06-draft-dev-lint-final-verification.md

다음 공통 규칙을 따른다.
- `prompts/common-rules.md`
- `prompts/modes/development.md`

작업 목표:
- 앞선 lint 수정 작업 이후 전체 lint 통과 여부를 최종 확인하고 필요한 최소 후속 수정을 한다.

배경:
- 기대 결과는 `npm run lint`가 오류 없이 통과하는 것이다.
- 앞선 작업에서 여러 파일의 타입과 export 구조가 변경되었을 수 있다.

포함 범위:
- `npm run lint`를 실행한다.
- 새로 발생한 lint error가 있으면 원인을 확인하고 최소 범위로 수정한다.
- 가능하면 타입 검사 또는 빌드 명령이 프로젝트에 있는지 확인하고, 필요한 경우 실행한다.
- 최종 변경 파일과 검증 결과를 요약한다.

제외 범위:
- lint와 무관한 기능 개선은 하지 않는다.
- 의존성 버전 변경은 하지 않는다.
- 대규모 리팩터링은 하지 않는다.

제약:
- 기존 사용자 변경사항이 있으면 되돌리지 않는다.
- 검증 중 실패가 발생하면 실패 명령, 핵심 오류, 남은 작업을 명확히 정리한다.

명명 규칙:
- 새 파일 생성은 원칙적으로 하지 않는다.
- 불가피하게 추가할 경우 기존 naming pattern을 따른다.

완료 조건:
- `npm run lint`가 오류 없이 통과한다.
- 남은 warning이 있다면 파일과 이유를 정리한다.
- 실행한 검증 명령과 결과를 최종 보고한다.
